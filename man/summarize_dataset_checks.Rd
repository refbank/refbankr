% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{summarize_dataset_checks}
\alias{summarize_dataset_checks}
\title{Compute per-condition summary statistics and round-level plot data}
\usage{
summarize_dataset_checks(trials, selections, messages, conditions)
}
\arguments{
\item{trials}{A trials tibble (as returned by \code{\link[=get_trials]{get_trials()}}) for a single dataset.}

\item{selections}{A selections tibble (as returned by \code{\link[=get_selections]{get_selections()}}) for the same dataset.}

\item{messages}{A messages tibble (as returned by \code{\link[=get_messages]{get_messages()}}) for the same dataset.}

\item{conditions}{A conditions tibble (as returned by \code{\link[=get_conditions]{get_conditions()}}) for the same dataset.}
}
\value{
A list with three tibbles:
\itemize{
\item \code{summary_stats} — one row per condition (games, players, rounds,
trials, images, option set size, coverage of selections/messages).
\item \code{accuracy_plot_data} — one row per trial (\code{round_num}, \code{condition_label},
\code{stage_num}, \code{value}), \code{value} is 1 if the matcher selected the target image.
\item \code{words_plot_data} — one row per trial, \code{value} is the describer's word count.
}
}
\description{
Applies refbank's standard exclusions (trial-level \code{exclude},
message-level \code{message_irrelevant}) and summarizes accuracy and describer
word count by round, plus overall per-condition counts (games, players,
rounds, trials, images). This is the shared computation behind
refbank-import's interactive \code{check_dataset()} QA tool and the refbank
website's per-dataset validation page, so the numbers shown in both
places are guaranteed to match.
}
